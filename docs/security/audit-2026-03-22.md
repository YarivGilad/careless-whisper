# Security Audit — Careless Whisper
**Date:** 2026-03-22
**Scope:** Dependency CVE audit + static code review (Option B)
**Audience:** Beginner — all findings explained in plain English

---

## Dependency Audit

### Rust (cargo audit)

`cargo` and `cargo-audit` were not available in the CI/shell environment used for this audit (Rust toolchain not on PATH). The audit command attempted was:

```
cd src-tauri && cargo audit 2>&1
```

Result: `cargo not found`

**Resolved crate versions from `src-tauri/Cargo.lock` (key direct dependencies):**

| Crate | Resolved Version |
|-------|-----------------|
| tauri | 2.x (see lock) |
| tauri-plugin-global-shortcut | 2.x |
| tauri-plugin-autostart | 2.x |
| tauri-plugin-opener | 2.x |
| whisper-rs | 0.12.x |
| cpal | 0.15.x |
| rubato | 0.15.x |
| arboard | 3.x |
| tokio | 1.x |
| reqwest | 0.12.x |
| serde | 1.x |
| simplelog | 0.12.x |
| objc2 (macOS) | 0.6.x |
| windows (Windows) | 0.58.x |
| libc (Linux) | 0.2.x |

**Action required:** Run `cargo audit` in `src-tauri/` on a machine with the Rust toolchain installed to obtain CVE findings. Add results here before finalising this report.

### npm/Node (pnpm audit)

`pnpm` was not available in the CI/shell environment used for this audit. The audit command attempted was:

```
pnpm audit 2>&1
```

Result: `pnpm not found`

**Resolved npm package versions from `pnpm-lock.yaml`:**

| Package | Resolved Version |
|---------|-----------------|
| @tauri-apps/api | 2.10.1 |
| @tauri-apps/plugin-opener | 2.5.3 |
| @tauri-apps/cli | 2.10.1 |
| react | 19.2.4 |
| react-dom | 19.2.4 |
| vite | 7.3.1 |
| typescript | 5.8.3 |
| @vitejs/plugin-react | 4.7.0 |
| tailwindcss | 3.4.19 |
| postcss | 8.5.8 |
| autoprefixer | 10.4.27 |

**Action required:** Run `pnpm audit` in the project root on a machine with pnpm installed to obtain CVE findings. Add results here before finalising this report.

---

## Static Code Review

### 1. Command Injection (Linux shell-out)
### 2. IPC Input Validation
### 3. CSP Disabled + get_recent_logs
### 4. Unsafe Rust
### 5. Overly Broad Permissions / macOSPrivateApi
### 6. Secrets / Credentials
### 7. Settings Persistence
### 8. Model Download Integrity
### 9. Linux FIFO Listener

---

## Summary Table

| # | Severity | Title | File |
|---|----------|-------|------|
_pending_

## Recommended Fix Order

_pending_
